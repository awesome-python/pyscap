#!/bin/bash

. lib/oracle.sh

notchecked "The DBMS data files, transaction logs or audit files are not stored in dedicated directories or disk partitions separate from software or other application files"